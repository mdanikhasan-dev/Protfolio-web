# Open questions

These questions are unresolved after comparing the authentic source, current repository, reference
folders, and prior-session work. They are not permission to invent a fallback claim.

## Identity and contact

1. Is `mdanikhasan.com` the approved canonical production domain? The current site uses it, but the
   authentic source file does not establish it.
2. What public email or form destination may the Contact page use? Until approved, publish no
   private email and make no claim that a form submits.
3. Should the HTML resume include education dates, work dates, or other records not present in the
   current source? No date should be inferred.
4. Is remote availability an approved explicit statement, or should remote discovery remain an SEO
   intent only?

## Project evidence

5. Are the existing SoctuKit and UIU Discord Bot repository URLs approved, current, and public?
6. Is it approved to state that the SoctuKit public release repository contains no source code? The
   current site says this, but the authentic source does not.
7. Is `src/assets/projects/soctukit-settings.png` an authentic, user-supplied product screenshot?
   Its provenance is not recorded, so it must not be treated as evidence yet.
8. Which approved visual should represent each of Boilabin, UIU Discord Bot, Salty Potato AI, and
   SoctuKit after the art direction is chosen?
9. Which project facts beyond the source file may be supported by repository evidence in a later
   technical audit?

## Content and information architecture

10. Should the existing `/writing/` URLs be redirected to the required public label `/stories/`, or
    should the path remain `/writing/` while navigation says Stories?
11. Should `/hire/` be consolidated into `/contact/`? Maintaining both currently duplicates the
    project-intake job.
12. Should `/services/native-windows-software/` remain as a supporting capability page, or be folded
    into Custom Software Development so the public offer stays limited to two services?
13. Are `/now/` and `/open-source/` useful enough to keep as independent routes?
14. Is the optional Boilabin seller-interest page wanted in the first complete release?
15. Is `/resume/` wanted, and what verified information may it contain?

## Visual identity and play

16. No portrait is approved. What size and aspect-ratio constraints should the replaceable portrait
    slot anticipate?
17. Which original art direction will be approved at Checkpoint 2?
18. Which separate game concept will be approved at Checkpoint 4?
19. Is audio intentionally excluded permanently, or only until a later explicit request?

## Reference package

20. Website 01 contains 6,547 files numbered within a `000001`–`007047` range rather than the
    expected 7,047 files. The exact missing range is recorded by the forensic index; should the
    missing source frames be recovered before art direction work?
21. The first three reference directories are named `Website 1`, `Website 2`, and `Website 3`, not
    the underscore names in the prompt. Should the folders remain as-is now that the analysis script
    supports both conventions?
22. The source-of-truth and master prompt are in the supplied master-package directory, not directly
    under `MYport`. Should copies be placed beside the four references for future sessions?

## Tooling required before later checkpoints

23. May the later motion-proof checkpoint install `ffmpeg` for demonstration video encoding?
24. May the later 3D checkpoint install a glTF optimisation tool such as `gltfpack` or
    `@gltf-transform/cli`?
25. Should the repository download Playwright-managed browser binaries, or continue using the
    installed Chrome/Edge executables?
