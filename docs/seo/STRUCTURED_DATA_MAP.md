# Structured data map

Structured data must match visible content. It is not added merely to chase a rich result.

| Route type                      | Schema                                                                  |
| ------------------------------- | ----------------------------------------------------------------------- |
| All public pages                | `Person` for MD Anik Hasan, including only the verified GitHub `sameAs` |
| Home                            | `WebSite`                                                               |
| About                           | `ProfilePage`                                                           |
| Service detail                  | `Service` and `BreadcrumbList`                                          |
| Nested project and seller pages | `BreadcrumbList`                                                        |
| SoctuKit and UIU Discord Bot    | `SoftwareApplication` using only visible facts                          |
| Writing detail                  | `BlogPosting` and `BreadcrumbList`                                      |

Boilabin does not receive claims about launch, customers, sellers, revenue, or organization scale.
Salty Potato AI does not receive production software or benchmark claims. Search and 404 are
noindexed.
