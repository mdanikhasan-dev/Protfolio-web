# Hinode browser runtime

This is the isolated clean-room browser implementation. Modules may depend on general framework
infrastructure and Three.js, but may not import a rejected game module or public asset.

