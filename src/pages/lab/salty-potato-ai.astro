---
import { getCollection } from 'astro:content';
import ProjectDetail from '../../components/ProjectDetail.astro';

const project = (await getCollection('projects')).find((entry) => entry.id === 'salty-potato-ai');
if (!project) throw new Error('Salty Potato AI content is missing.');
---

<ProjectDetail project={project} />
