{
  "name": "mdanikhasan-site",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node build/site.js",
    "preview": "npx serve dist",
    "validate:html": "npx html-validate \"dist/**/*.html\" \"dist/*.html\"",
    "check": "node -e \"require('./src/data/site'); require('./src/data/pages'); console.log('Config OK');\""
  },
  "engines": {
    "node": "20.x"
  }
}
